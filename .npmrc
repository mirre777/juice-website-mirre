# Use npm for package management
package-manager=npm

# Disable package-lock generation for faster installs
package-lock=true

# Use exact versions
save-exact=true

# Disable funding messages
fund=false

# Disable audit warnings in CI
audit=false

# Allow legacy peer dependencies
legacy-peer-deps=true
